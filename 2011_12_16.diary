#

....早上弄了一上午，才发现原来是testset_query_num不一致造成的，原来是100，新代码读配置文件是10，我艹；再看看建索引与读索引结果一致不了？....
....直接建索引和load索引文件，结果不一致，下一步测试mplsh看看是什么原因造成的....
....终于发现问题了，data load进来之后在insert到index里之前先normalize一下，然后当load index时候，正常读取dataset却没有normalize，dataset是原始数据文件，每次index.query时候还要用dataset....
....遇到个问题，就是读取文件时候，fseek参数offset是size_t类型，如果是大文件显然不能直接一次fseek到某个偏移位置，需要多次fseek；这样写了的话，出现了bug，还不知到什么原因；后来发现是读取错了dataset，多个dataset不一致还真是有很大潜在问题....
